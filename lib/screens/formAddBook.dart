import 'package:flutter/material.dart';
import '../models/bookModels.dart';

class FormAddBookPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Form Add Book"),
      ),
    );
  }

}